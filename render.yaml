services:
  - type: web
    name: linkedin-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: frontend/dist

    envVars:
      - key: NODE_VERSION
        value: 18
